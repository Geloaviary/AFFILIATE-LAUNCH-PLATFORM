const {
  scoreAsset
} = require(
  "./score-asset"
);

const {
  scoreSceneAssets
} = require(
  "./score-scene-assets"
);

const {
  selectBestAssets
} = require(
  "./select-best-assets"
);

module.exports = {

  scoreAsset,

  scoreSceneAssets,

  selectBestAssets

};