warning: in the working copy of 'api/utils.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/api/utils.js b/api/utils.js[m
[1mindex 10bd8d6..85b665c 100644[m
[1m--- a/api/utils.js[m
[1m+++ b/api/utils.js[m
[36m@@ -1,4 +1,7 @@[m
 const fetch = require("node-fetch");[m
[32m+[m[32mconst Runtime = require([m
[32m+[m[32m  "../lib/departments/runtime"[m
[32m+[m[32m);[m
 const { kv } = require("@vercel/kv");[m
 [m
 const {[m
[36m@@ -11,6 +14,32 @@[m [mconst OPENAI_KEY = (process.env.OPENAI_API_KEY || "").replace(/[^\x20-\x7E]/g, "[m
 [m
 exports.default = async function handler(req, res) {[m
   const { action } = req.query;[m
[32m+[m[32m  // ============ RUNTIME OBSERVATION ============[m
[32m+[m[32m  if (action === "runtime-observation") {[m
[32m+[m[32m    try {[m
[32m+[m[32m      const campaignId =[m
[32m+[m[32m        typeof req.query.campaignId === "string" &&[m
[32m+[m[32m        req.query.campaignId.trim()[m
[32m+[m[32m          ? req.query.campaignId.trim()[m
[32m+[m[32m          : null;[m
[32m+[m
[32m+[m[32m      const observation =[m
[32m+[m[32m        await Runtime.observe({[m
[32m+[m[32m          campaignId[m
[32m+[m[32m        });[m
[32m+[m
[32m+[m[32m      return res.status(200).json(observation);[m
[32m+[m[32m    } catch (error) {[m
[32m+[m[32m      console.error([m
[32m+[m[32m        "[RUNTIME OBSERVATION ERROR]",[m
[32m+[m[32m        error[m
[32m+[m[32m      );[m
[32m+[m
[32m+[m[32m      return res.status(500).json({[m
[32m+[m[32m        error: error.message[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
 [m
   // ============ TRENDING SOUNDS ============[m
   if (action === "trending") {[m
[36m@@ -63,7 +92,7 @@[m [mexports.default = async function handler(req, res) {[m
   if (action === "reply-comments") {[m
     console.log("Comment webhook:", req.body);[m
     const { comment, username, platform } = req.body;[m
[31m-    const reply = `Thanks @${username}! Check the link in our bio for the best deal! 🔥`;[m
[32m+[m[32m    const reply = `Thanks @${username}! Check the link in our bio for the best deal! ≡ƒöÑ`;[m
     return res.status(200).json({ success: true, reply, platform });[m
   }[m
 [m
